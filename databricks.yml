# This is a Databricks asset bundle definition for my_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: f1_project
  uuid: 9836fa05-0917-4013-b751-8bac0c919209

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-59bc96e6-d850.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-59bc96e6-d850.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/bence.balogh@datapao.com to make sure we only have a single copy.
      root_path: /Workspace/Users/bence.balogh@datapao.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: bence.balogh@datapao.com
        level: CAN_MANAGE
